Binary on/off toggle; the checked track glows cyan.

```jsx
<Switch label="Show declining topics" defaultChecked />
<Switch checked={live} onChange={e => setLive(e.target.checked)} label="Live index" />
```
