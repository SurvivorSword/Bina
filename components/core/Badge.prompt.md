Tiny uppercase mono pill for status and metadata — momentum direction, "featured", counts.

```jsx
<Badge tone="rise" dot>Trending</Badge>
<Badge tone="gold">Editor's pick</Badge>
<Badge>2017</Badge>
```

Tones: `neutral` `rise` `fall` `info` `gold`. `dot` adds a glowing status dot.
