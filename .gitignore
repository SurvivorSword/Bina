# ── OS / editor junk ───────────────────────────────
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# ── Dependencies (none committed) ──────────────────
node_modules/

# ── Original upload kept private (working copy lives in assets/) ──
#uploads/

# ── Platform-internal build metadata (regenerated by the design tool) ──
# NOTE: _ds_bundle.js is intentionally NOT ignored — the HTML demos
# (component cards + the Bina app) need it to render standalone.
_ds_manifest.json
_adherence.oxlintrc.json
